<!DOCTYPE html>
<html lang="en">

<head>
    <title>Add Hall - EDUCATION MOODLE (Edu-Mo)</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            font-family: 'Nunito', 'sans-serif';
            background-color: #f4f7fc;
            color: #09090a;
        }

        .navbar {
            background-color: #007bff;
        }

        .navbar-brand,
        .navbar-nav .nav-link {
            color: #ffffff !important;
        }

        .bg-primary {
            background-color: #007bff !important;
            color: #ffffff;
        }

        .form-container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
        }

        .form-control {
            border-radius: 5px;
        }

        .btn-outline-secondary {
            color: #007bff;
            border-color: #007bff;
        }

        .btn-outline-secondary:hover {
            color: #ffffff;
            background-color: #007bff;
            border-color: #007bff;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row bg-primary  text-white">
            <div class="col-12">
                <h1 class="text-center">Edu-Mo</h1>
            </div>
        </div>

        <div id="add-hall">
            <div class="row bg-primary p-3 text-white">
                <div class="col-12">
                    <h2 class="text-center">Add Hall</h2>
                </div>
            </div>

            <!-- Add Hall Form Section -->
            <div class="my-5 form-container">
                <form action="/adminSide/addHall" method="POST">
                    <div class="mb-3">
                        <label for="hallName" class="form-label">Hall Name</label>
                        <input type="text" class="form-control" id="hallName" name="hallName" required>
                    </div>
                    <div class="mb-3">
                        <label for="provost" class="form-label">Provost Name</label>
                        <select class="form-select" id="provost" name="provost" required>
                            <option value="" disabled selected>Select Provost Name</option>
                            <% AllDeptNameInfo.forEach(row => { %>
                                <option value="<%= row.DEPT_NAME %>"><%= row.DEPT_NAME %></option>
                            <% }); %>
                            <!-- Department options will be dynamically populated by JavaScript -->
                        </select>
                    </div>
                    

                    <!-- Add more form elements as needed -->
                    <div class="small-box">
                        <button type="submit" class="btn btn-outline-secondary">Add Hall</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

   
</body>

</html>
